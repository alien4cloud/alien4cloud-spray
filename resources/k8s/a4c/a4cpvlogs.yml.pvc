kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-a4c-logs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ pvlogssize }}
  selector:
     matchLabels:
        app: a4c-logs
