apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-consul-volume-consul-2
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ pvsize }}
  volumeMode: Filesystem
  volumeName: pv-consul0
