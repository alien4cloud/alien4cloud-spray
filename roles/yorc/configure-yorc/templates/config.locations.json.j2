{
  "locations": [
    { "name": "awsLocation",
      "type": "aws",
      "properties": {
        "access_key": "{{ AWS_ACCESS_KEY }}",
        "secret_key": "{{ AWS_SECRET_KEY }}",
        "region": "{{ AWS_REGION }}"
      }
    }
  ]
}
